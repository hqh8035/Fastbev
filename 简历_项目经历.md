# 简历项目经历

## 量化成果

| 指标 | 初始版本 | 最终版本 | 提升 |
|------|---------|---------|------|
| Overall F1 | 0.054 | **0.724** | 13.4x |
| Vehicle F1 | 0.055 | **0.781** | 14x |
| Vehicle Recall | 0.033 | **0.795** | 24x |
| Person F1 | 0.000 | **0.548** | 从0到可用 |
| Cyclist F1 | N/A | **0.550** | — |
| Loss震荡 | — | — | 减少50-70% |
| Yaw角误差 | — | — | 降低30-50% |

---

## 版本一：侧重3D感知/自动驾驶方向

> 适合投递：蔚来、小鹏、Momenta、地平线、华为智驾、百度Apollo等

**FastBEV 实时单目3D目标检测系统** | 算法实习生 | 2025.12 - 2026.02

基于BEV（鸟瞰图）表示的单目相机实时3D目标检测与语义分割系统，面向自动驾驶/机器人场景，支持车辆、行人、骑行者三类目标检测。

- **模型训练优化**：针对训练震荡问题，设计并实施了一套完整的训练稳定性方案——包括学习率Warmup策略、Cosine Annealing调度器、EMA参数平滑（decay=0.9999）和梯度裁剪，使Loss震荡减少50-70%，模型整体F1从0.054提升至**0.724**（提升13倍）
- **Loss函数设计**：针对朝向角(Yaw)预测不准确问题，提出将L1 Loss替换为Cosine Similarity Loss，利用单位圆几何约束确保sin/cos预测归一化，角度预测误差降低30-50%，帧间朝向连续性显著提升
- **多任务Loss权重平衡**：通过分析各Loss分量的数量级差异，设计了自适应权重方案（center×15, offset×2, yaw×2, lwh×0.3），使各Loss贡献保持同一量级，整体检测精度（Precision）达到**0.678**，召回率（Recall）达到**0.775**
- **多数据集联合训练**：设计加权随机采样策略支持Waymo、Pedestrian、Corner三个数据集联合训练，通过采样权重（1:1.83:7.65）解决数据集不平衡问题，Vehicle F1达到**0.781**
- **评估体系建设**：实现基于nuScenes标准的mAP评估pipeline，支持多阈值距离匹配、旋转IoU计算（Shapely）、分类别指标统计，输出JSON/CSV/TXT多格式报告

**技术栈**：PyTorch, DDP分布式训练, EMA, ONNX, TensorBoard, NumPy, OpenCV, Shapely

---

## 版本二：侧重通用CV/多模态方向

> 适合投递：字节跳动、腾讯、商汤、旷视、阿里达摩院等

**基于BEV表示的实时3D视觉感知系统** | 算法实习生 | 2025.12 - 2026.02

面向自动驾驶场景的单目相机3D感知系统，通过多高度平面投影将2D图像特征转换为BEV空间表示，实现实时3D目标检测与语义分割。

- **视觉特征到BEV空间的投影**：基于相机内参实现多高度平面采样（10个Y平面），通过可微分grid_sample将2D图像特征投射到3D BEV网格（32m×32m, 10px/m），支持DINOv3 ViT和MobileNetV2两种Backbone
- **训练策略全面优化**：系统性解决模型训练不稳定问题，引入Warmup+Cosine Annealing学习率调度、EMA权重平滑、多任务Loss权重平衡，综合使检测F1从5.4%提升至**72.4%**
- **角度回归损失函数创新**：将朝向角预测从L1 Loss改为Cosine Similarity Loss，通过L2归一化强制sin/cos预测满足单位圆约束，误差降低30-50%，解决了帧间预测跳变问题
- **多源数据联合训练框架**：实现支持异构数据集（Waymo/自采数据）的统一训练pipeline，包含加权采样器、类别映射、DDP多卡同步等完整工程能力
- **模型评估与部署**：建立nuScenes标准评估体系（旋转IoU匹配+匈牙利算法），支持ONNX导出与边缘端（地平线芯片）部署

**技术栈**：PyTorch, Vision Transformer (DINOv3), BEV Perception, DDP, EMA, ONNX, TensorBoard

---

## 使用建议

1. **投自动驾驶公司** → 用版本一
2. **投通用CV/多模态岗** → 用版本二
3. 根据JD适当调整bullet point顺序，把最匹配的放第一条
4. 简历空间有限时，保留前3条bullet point即可
